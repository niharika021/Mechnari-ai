google-adk>=2.7,<3
google-genai  # pulled in by google-adk; used for types.Content
google-cloud-bigquery
google-cloud-firestore  # review queue + reports (both fall back to a JSON file)
firebase-admin  # verifies Google sign-in ID tokens (auth.py); sign-in is optional
ag-ui-adk>=0.7,<1  # AG-UI protocol wrapper around the ADK agent (api.py mounts it)

# api.py imports these directly. They also arrive transitively via
# google-adk, but depending on that is luck, not a contract - if ADK ever
# drops them the container still has to serve.
fastapi
uvicorn

streamlit
python-dotenv
pandas
scikit-learn
